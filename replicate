#include <iostream>

using namespace std;

int main()
{
    int age;
    cout<<"enter the age of the person";
    cin>>age;
    if(age<=18)
    {
    cout<<"not elligible";
    cout<<"an adult";
    }
    else{
    cout<<"elligible  ";
    cout<<" not an adult";
    }
}
